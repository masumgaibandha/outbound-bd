/** Anchor ids shared by the /agencies page's server sections and its
 * client-side scroll/sticky-CTA components. */
export const GET_DETAILS_ID = "get-details";

/** The hero section, whose own "Get the details" button makes the sticky one redundant. */
export const HERO_ID = "agency-hero";

/** Marks the final CTA so the sticky mobile button can step aside once it's reached. */
export const FINAL_CTA_ID = "agency-final-cta";
