# MongoDB connection string (used by Mongoose)
MONGODB_URI=mongodb://127.0.0.1:27017/outbound-bd

# Public app URL — used for metadataBase, sitemap.xml, and robots.txt
NEXT_PUBLIC_APP_URL=http://localhost:3000
